services:
  jvshomecontrol:
    image: username/jvshomecontrol:beta
    build: .
    container_name: jvshomecontrol
    restart: unless-stopped
    ports:
      - "80:80"      # HTTP  (default)
      - "443:443"    # HTTPS (after enabling in Settings → Server)
    volumes:
      # Persist config, backgrounds, certs, and sounds across container restarts
      - jvs-data:/app/server/data

volumes:
  jvs-data:
    name: jvs-data
